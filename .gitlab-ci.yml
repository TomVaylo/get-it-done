stages:
  - build
  - deploy

pages:
  stage: deploy
  image: node:20
  cache:
    paths:
      - node_modules/
  script:
    - npm install
    - npm run build
    - mkdir public
    - cp -r dist/* public/
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == "main"